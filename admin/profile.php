<?php require_once __DIR__ . '/../inc/auth.php'; require_once __DIR__ . '/../inc/csrf.php'; require_admin();
$stmt=$pdo->prepare("SELECT * FROM admin WHERE username=? LIMIT 1"); $stmt->execute([$_SESSION['admin_user']]); $user=$stmt->fetch(); $msg='';
if($_SERVER['REQUEST_METHOD']==='POST' && ($_POST['action'] ?? '')==='update'){ if(!verify_csrf($_POST['csrf'] ?? '')) die('Invalid CSRF'); $email=trim($_POST['email'] ?? ''); $username=trim($_POST['username'] ?? ''); if($username==='') $msg='Username cannot be empty.'; else { $pdo->prepare("UPDATE admin SET username=?, email=? WHERE id=?")->execute([$username,$email,$user['id']]); $_SESSION['admin_user']=$username; $msg='Profile updated.'; $stmt=$pdo->prepare("SELECT * FROM admin WHERE id=?"); $stmt->execute([$user['id']]); $user=$stmt->fetch(); } }
?><!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Profile - Admin</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<!-- Modern Luxury Theme -->
<link rel="stylesheet" href="assets/css/theme.css">
</head><body><div class="container py-4"><a href="dashboard.php">« Back</a><h3>Profile</h3><?php if($msg): ?><div class="alert alert-success"><?=htmlspecialchars($msg)?></div><?php endif; ?><form method="post"><input type="hidden" name="csrf" value="<?=csrf_token()?>"><input type="hidden" name="action" value="update"><div class="mb-3"><label>Username</label><input name="username" class="form-control" value="<?=htmlspecialchars($user['username'])?>" required></div><div class="mb-3"><label>Email</label><input name="email" class="form-control" value="<?=htmlspecialchars($user['email'] ?? '')?>"></div><button class="btn btn-primary">Save</button></form></div>
<script src="assets/js/theme.js"></script>
</body></html>